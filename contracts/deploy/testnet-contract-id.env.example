# After deploy, copy to testnet-contract-id.env (gitignored) or merge into server .env.
# Generate with: ./contracts/scripts/deploy-testnet.sh
SOROBAN_AGENT_MARKET_CONTRACT_ID=REPLACE_WITH_CONTRACT_ID_FROM_STELLAR_CLI
